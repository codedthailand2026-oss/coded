# ===================================
# Supabase Configuration
# ===================================
# คัดลอกไฟล์นี้เป็น .env.local แล้วกรอก values จริง

# Supabase Project URL
# หาได้จาก: Supabase Dashboard → Project Settings → API → Project URL
# Format: https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Supabase Anon (Public) Key
# หาได้จาก: Supabase Dashboard → Project Settings → API → Project API keys → anon public
# Key นี้ปลอดภัยที่จะใช้ฝั่ง client (browser)
# Row Level Security (RLS) ป้องกันการเข้าถึงข้อมูลที่ไม่ได้รับอนุญาต
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===================================
# Notes
# ===================================
# 1. ไฟล์ .env.local จะไม่ถูก commit ขึ้น git (อยู่ใน .gitignore)
# 2. NEXT_PUBLIC_ = ตัวแปรนี้สามารถเข้าถึงได้จาก browser
# 3. ตัวแปรที่ไม่มี NEXT_PUBLIC_ = server-side only
# 4. หลังแก้ไข .env.local ต้อง restart dev server (npm run dev)

# ===================================
# Stripe (สำหรับอนาคต)
# ===================================
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxx
# STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx
# STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# ===================================
# n8n (สำหรับอนาคต)
# ===================================
# N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/xxxxx
